<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ItemCheckoutActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_itemCheckoutList"
        android:layout_width="394dp"
        android:layout_height="457dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_weight="0" />

    <Button
        android:id="@+id/btn_scanCheckout"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_margin="5dp"
        android:text="Scan Item"
        app:layout_constraintEnd_toStartOf="@+id/btn_checkoutList"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_totalPrice" />

    <Button
        android:id="@+id/btn_checkoutList"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_margin="5dp"
        android:layout_marginTop="10dp"
        android:text="Checkout"
        app:layout_constraintBottom_toBottomOf="@+id/btn_scanCheckout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/btn_scanCheckout"
        app:layout_constraintTop_toBottomOf="@+id/tv_totalPrice" />

    <TextView
        android:id="@+id/tv_totalPrice"
        android:layout_width="401dp"
        android:layout_height="58dp"
        android:layout_margin="5dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="10dp"
        android:gravity="center_horizontal|center_vertical"
        android:text="Total Price"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rv_itemCheckoutList" />
</androidx.constraintlayout.widget.ConstraintLayout>